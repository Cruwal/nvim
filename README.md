# The neovim config files

### TODO
- Precisa ajustar os plugins para `init.lua` dos plugins. Está quebrando na instalação (`require'nvim-treesitter/nvim-treesitter'` e o demais, porque o plugin ainda não foi baixado) 
